/*
malloc = memory allocation(申请空间(连续))
calloc = contiguous allocation(申请空间+数据初始化)
realloc = re-allocation(修改空间大小)
free(释放空间)
stdlib.h
*/
#include<stdlib.h>
#include<stdio.h>
int main()
{
    

    return 0;
}

