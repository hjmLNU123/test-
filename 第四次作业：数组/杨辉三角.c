/*输出一个以下的杨辉三角形，要求输出10行
77
12 1
1 33 1
1 464 1
*/
#include <stdio.h>

int main() {
    int t[10][10];
    // 首行和末行（每行的第一个和最后一个元素）的初始化
    for (int i = 0; i < 10; i++) {
        t[i][0] = 1;
        t[i][i] = 1;
    }
    // 后续行中间元素的计算
    for (int i = 2; i < 10; i++) {
        for (int j = 1; j < i; j++) {
            t[i][j] = t[i - 1][j - 1] + t[i - 1][j];
        }
    }
    // 杨辉三角的输出
    for (int i = 0; i < 10; i++) {
        for (int j = 0; j <= i; j++) {
            printf("%d ", t[i][j]);
        }
        printf("\n");
    }
    return 0;
}
//代码说明
数组初始化：通过第一个 for 循环，将二维数组 t 的每一行的第一个元素（t[i][0]）和最后一个元素（t[i][i]）都初始化为 1，这符合杨辉三角每行首尾元素为 1 的特点。
中间元素计算：第二个嵌套 for 循环从第 3 行（i = 2）开始，计算每行中间的元素。根据杨辉三角的规则，中间元素 t[i][j] 等于上一行前一列元素 t[i - 1][j - 1] 与上一行同列元素 t[i - 1][j] 之和。
输出杨辉三角：第三个嵌套 for 循环用于输出杨辉三角。外层循环控制行，内层循环控制每行输出的元素个数（每行输出的元素个数等于行号加 1），最后通过 printf("\n") 实现换行，使输出呈现三角形状。